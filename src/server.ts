import { appConstant } from "src/app";

console.log("Template for NodeJS project with TypeScript and ExpressJS");

const ROOT_DIR = appConstant.Defaults.ROOT_DIR
console.log(`Root directory: ${ROOT_DIR}`);
