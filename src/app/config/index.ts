// How to load the config file by the environment? E.g: base.toml -> development.toml -> development.local.toml
// We can the variable environment from the appConfig.Defaults.ENV
// How to define a sequence file variable?

const config = {};

export default config;